services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: flua
      POSTGRES_PASSWORD: flua
      POSTGRES_DB: flua_xml
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
